#!/bin/bash
# transition - Add transition between two videos
# Usage: ./transition <video1> <video2> [type:fade|slide|wipe]
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
"$SCRIPT_DIR/produce" transition "$@"
