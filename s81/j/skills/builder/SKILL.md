# builder SKILL

Regla permanente: después de cada tarea completada, hacer git commit con mensaje descriptivo (qué cambió y por qué). Si no hay git init, hacer git init + .gitignore estándar primero.
