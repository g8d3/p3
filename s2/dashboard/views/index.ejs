<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BizBot Command Center</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg: #0f172a;
            --surface: #1e293b;
            --primary: #8b5cf6;
            --text: #f8fafc;
            --text-muted: #94a3b8;
            --accent: #38bdf8;
        }

        body {
            font-family: 'Outfit', sans-serif;
            background-color: var(--bg);
            color: var(--text);
            margin: 0;
            padding: 0;
            min-height: 100vh;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 2rem;
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 3rem;
        }

        h1 {
            font-size: 2.5rem;
            font-weight: 600;
            background: linear-gradient(to right, var(--primary), var(--accent));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin: 0;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
            margin-bottom: 3rem;
        }

        .card {
            background: var(--surface);
            border-radius: 16px;
            padding: 1.5rem;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
            border: 1px solid rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(10px);
        }

        .card h3 {
            margin-top: 0;
            color: var(--text-muted);
            font-size: 0.875rem;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        .card .value {
            font-size: 2.5rem;
            font-weight: 600;
        }

        section {
            margin-bottom: 3rem;
        }

        h2 {
            font-size: 1.5rem;
            margin-bottom: 1.5rem;
            border-left: 4px solid var(--primary);
            padding-left: 1rem;
        }

        table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0 0.5rem;
        }

        th {
            text-align: left;
            color: var(--text-muted);
            padding: 1rem;
            font-weight: 400;
        }

        td {
            background: rgba(30, 41, 59, 0.7);
            padding: 1rem;
            first-child: border-radius: 8px 0 0 8px;
            last-child: border-radius: 0 8px 8px 0;
        }

        tr td:first-child { border-radius: 8px 0 0 8px; }
        tr td:last-child { border-radius: 0 8px 8px 0; }

        .btn {
            background: var(--primary);
            color: white;
            border: none;
            padding: 0.5rem 1rem;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.2s;
            font-weight: 500;
        }

        .btn:hover {
            opacity: 0.9;
            transform: translateY(-1px);
        }

        .btn-sm {
            padding: 0.25rem 0.75rem;
            font-size: 0.875rem;
        }
        
        .tag {
            display: inline-block;
            padding: 0.25rem 0.5rem;
            border-radius: 4px;
            font-size: 0.75rem;
            background: rgba(255,255,255,0.1);
        }

        .form-group {
            margin-bottom: 1rem;
        }

        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            color: var(--text-muted);
            font-size: 0.875rem;
        }

        .form-group input, .form-group select {
            width: 100%;
            padding: 0.5rem;
            background: rgba(30, 41, 59, 0.7);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 8px;
            color: var(--text);
            font-family: 'Outfit', sans-serif;
        }

        .form-group input:focus, .form-group select:focus {
            outline: none;
            border-color: var(--primary);
        }

        .config-actions {
            display: flex;
            gap: 0.5rem;
        }

        .active-config {
            border: 2px solid var(--primary);
        }

        .status-badge {
            padding: 0.25rem 0.5rem;
            border-radius: 12px;
            font-size: 0.75rem;
            font-weight: 500;
        }

        .status-active {
            background: rgba(74, 222, 128, 0.2);
            color: #4ade80;
        }

        .status-inactive {
            background: rgba(148, 163, 184, 0.2);
            color: var(--text-muted);
        }

        .header-controls {
            display: flex;
            align-items: center;
            gap: 1rem;
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <div class="header-controls">
                <h1 style="margin: 0;">BizBot // Dashboard</h1>
            </div>
            <div class="header-controls">
                <div class="status-indicator">
                    <span style="color: #4ade80">●</span> System Online
                </div>
                <div style="display: flex; gap: 0.5rem;">
                    <form action="/rescape" method="POST" style="display:inline;">
                        <button type="submit" class="btn btn-sm">🔄 Rescape</button>
                    </form>
                    <form action="/reset-data" method="POST" style="display:inline;" 
                          onsubmit="return confirm('Are you sure you want to reset all data? This will delete all ideas and assets.')">
                        <button type="submit" class="btn btn-sm" style="background: #dc2626;">🗑️ Reset Data</button>
                    </form>
                </div>
            </div>
        </header>

        <div class="stats-grid">
            <div class="card">
                <h3>Ideas Scouted</h3>
                <div class="value"><%= stats.ideas %></div>
            </div>
            <div class="card">
                <h3>Products Created</h3>
                <div class="value"><%= stats.assets %></div>
            </div>
            <div class="card">
                <h3>Revenue</h3>
                <div class="value">$0.00</div>
            </div>
        </div>

        <section>
            <h2>Incoming Ideas</h2>
            <table>
                <thead>
                    <tr>
                        <th>Source</th>
                        <th>Title</th>
                        <th>Status</th>
                        <th>Score</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% ideas.forEach(idea => { %>
                    <tr>
                        <td><span class="tag"><%= idea.source %></span></td>
                        <td><%= idea.title.substring(0, 60) %>...</td>
                        <td>
                            <span class="status-badge <%= idea.status === 'new' ? 'status-inactive' : idea.status === 'processed' ? 'status-active' : 'status-inactive' %>">
                                <%= idea.status %>
                            </span>
                        </td>
                        <td><%= idea.score %></td>
                        <td>
                            <div style="display: flex; gap: 0.5rem;">
                                <form action="/ideas/<%= idea.id %>/approve" method="POST" style="display:inline">
                                    <button class="btn btn-sm">Generate Product</button>
                                </form>
                                <form action="/recreate/<%= idea.id %>" method="POST" style="display:inline">
                                    <button class="btn btn-sm" style="background: #f59e0b;">🔄 Recreate</button>
                                </form>
                            </div>
                        </td>
                    </tr>
                    <% }); %>
                    <% if (ideas.length === 0) { %>
                        <tr><td colspan="5" style="text-align:center; color: var(--text-muted)">No ideas found. Try rescraping to get new ideas.</td></tr>
                    <% } %>
                </tbody>
            </table>
        </section>

        <section>
            <h2>Portfolio Assets</h2>
            <table>
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Type</th>
                        <th>Status</th>
                        <th>Description</th>
                    </tr>
                </thead>
                <tbody>
                    <% assets.forEach(asset => { %>
                    <tr>
                        <td><%= asset.name.substring(0, 40) %>...</td>
                        <td><span class="tag"><%= asset.type %></span></td>
                        <td><%= asset.status %></td>
                        <td><%= asset.description.substring(0, 50) %>...</td>
                    </tr>
                    <% }); %>
                </tbody>
            </table>
        </section>

        <section>
            <h2>LLM Configuration</h2>
            
            <!-- Add New Configuration -->
            <div class="card" style="margin-bottom: 2rem;">
                <h3 style="margin-top: 0; color: var(--text);">Add New LLM Configuration</h3>
                <form action="/llm-config" method="POST">
                    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 1rem;">
                        <div class="form-group">
                            <label for="name">Configuration Name</label>
                            <input type="text" id="name" name="name" placeholder="e.g., OpenAI GPT-4" required>
                        </div>
                        <div class="form-group">
                            <label for="provider">Provider</label>
                            <select id="provider" name="provider" required>
                                <option value="">Select Provider</option>
                                <option value="openai">OpenAI</option>
                                <option value="anthropic">Anthropic (Claude)</option>
                                <option value="google">Google (Gemini)</option>
                            </select>
                        </div>
                        <div class="form-group">
                            <label for="apiKey">API Key</label>
                            <input type="password" id="apiKey" name="apiKey" placeholder="Your API key" required>
                        </div>
                        <div class="form-group">
                            <label for="model">Model (optional)</label>
                            <input type="text" id="model" name="model" placeholder="e.g., gpt-4o, claude-3-sonnet">
                        </div>
                        <div class="form-group" style="grid-column: 1 / -1;">
                            <label for="baseUrl">Base URL (optional)</label>
                            <input type="url" id="baseUrl" name="baseUrl" placeholder="Custom API endpoint URL">
                        </div>
                    </div>
                    <button type="submit" class="btn">Save Configuration</button>
                </form>
            </div>

            <!-- Existing Configurations -->
            <table>
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Provider</th>
                        <th>Model</th>
                        <th>Status</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    <% llmConfigs.forEach(config => { %>
                    <tr class="<%= config.is_active ? 'active-config' : '' %>">
                        <td><%= config.name %></td>
                        <td><span class="tag"><%= config.provider %></span></td>
                        <td><%= config.model || 'Default' %></td>
                        <td>
                            <span class="status-badge <%= config.is_active ? 'status-active' : 'status-inactive' %>">
                                <%= config.is_active ? 'Active' : 'Inactive' %>
                            </span>
                        </td>
                        <td>
                            <div class="config-actions">
                                <% if (!config.is_active) { %>
                                <form action="/llm-config/<%= config.id %>/activate" method="POST" style="display:inline">
                                    <button type="submit" class="btn btn-sm">Activate</button>
                                </form>
                                <% } %>
                                <form action="/llm-config/<%= config.id %>/delete" method="POST" style="display:inline" 
                                      onsubmit="return confirm('Are you sure you want to delete this configuration?')">
                                    <button type="submit" class="btn btn-sm" style="background: #dc2626;">Delete</button>
                                </form>
                            </div>
                        </td>
                    </tr>
                    <% }); %>
                    <% if (llmConfigs.length === 0) { %>
                        <tr><td colspan="5" style="text-align:center; color: var(--text-muted)">
                            No LLM configurations found. Add one above to enable AI-powered content generation.
                        </td></tr>
                    <% } %>
                </tbody>
            </table>
        </section>
    </div>
</body>
</html>
