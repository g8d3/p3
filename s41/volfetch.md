use joblib, hyperliquid python sdk and env vars:

HL_API_ADDRESS
HL_API_KEY
HL_API_URL

to create a python script that fetches and caches  volume of all hyperliquid assets in the past hour, output csv, volume in usd, and if API allows : seller volume and buyer volume.


