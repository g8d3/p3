#!/bin/bash
# sfx - Add sound effect to video
# Usage: ./sfx <video> <sfx_name> [at_seconds]
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
"$SCRIPT_DIR/produce" sfx "$@"
