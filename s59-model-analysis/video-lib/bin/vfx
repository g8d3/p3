#!/bin/bash
# vfx - Add visual effects/text overlay
# Usage: ./vfx <video> <text> [position] [start_time] [duration]
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
"$SCRIPT_DIR/produce" text "$@"
