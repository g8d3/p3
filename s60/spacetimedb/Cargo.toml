[package]
name = "user_tables"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
spacetimedb = "2"
log = "0.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
